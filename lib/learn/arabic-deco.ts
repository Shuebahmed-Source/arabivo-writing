import type { UnitId } from "@/lib/lessons";

/** Decorative Arabic watermark strings per unit (handoff — no sacred text). */
export const UNIT_ARABIC_DECO: Record<UnitId, string> = {
  letters: "أبجد",
  "letter-forms": "جـ ـجـ ـج",
  "simple-words": "سلام",
  "challenge-words": "صعب",
};
